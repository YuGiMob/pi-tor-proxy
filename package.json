{
  "name": "pi-tor-proxy",
  "version": "0.1.10",
  "description": "Pi extension that routes pi agent requests through Tor. It downloads and manages its own Tor binary, so nothing needs to be installed on the system.",
  "main": "index.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YuGiMob/pi-tor-proxy.git"
  },
  "author": "pi-tor-proxy contributors",
  "keywords": [
    "pi-package",
    "pi",
    "coding-agent",
    "extension",
    "tor",
    "privacy",
    "proxy",
    "anonymous"
  ],
  "license": "MIT",
  "files": [
    "index.ts",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {},
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  },
  "scripts": {},
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@types/node": "^22.0.0"
  }
}
